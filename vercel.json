{
  "framework": "nextjs",
  "buildCommand": "pnpm exec next build --webpack",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "functions": {
    "app/api/**/*.ts": {
      "maxDuration": 300
    }
  }
}
