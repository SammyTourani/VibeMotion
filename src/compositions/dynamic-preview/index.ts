/**
 * DynamicPreview Composition
 *
 * Export barrel for the data-driven preview composition.
 */

export {
  DynamicPreview,
  type DynamicPreviewProps,
  type DynamicPreviewTheme,
  type Scene,
  type TranscriptWord,
} from "./DynamicPreview";
